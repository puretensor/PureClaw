apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
  namespace: nexus
  labels:
    app: nexus
type: Opaque
stringData:
  # Values are placeholders -- apply real values via:
  #   kubectl create secret generic nexus-secrets -n nexus \
  #     --from-literal=MEMORY_PG_URL='postgresql://...' \
  #     --from-literal=TERMINAL_API_KEY='...' \
  #     --dry-run=client -o yaml | kubectl apply -f -
  MEMORY_PG_URL: "PLACEHOLDER_SET_VIA_KUBECTL"
  TERMINAL_API_KEY: "PLACEHOLDER_SET_VIA_KUBECTL"
